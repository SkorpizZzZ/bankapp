<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">
    <changeSet id="create-user-table" author="ilgiznigmatullin (generated)">
        <preConditions>
            <and>
                <not>
                    <tableExists tableName="users" schemaName="${schemaName}"/>
                </not>
            </and>
        </preConditions>
        <createTable tableName="users" schemaName="${schemaName}">
            <column name="user_id" type="bigserial">
                <constraints primaryKey="true"/>
            </column>
            <column name="login" type="varchar(255)">
                <constraints nullable ="false" unique = "true"/>
            </column>
            <column name="password" type="varchar(255)">
                <constraints nullable ="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable ="false"/>
            </column>
            <column name="birthdate" type="date">
                <constraints nullable ="false"/>
            </column>
        </createTable>
        <rollback>
            <dropTable tableName="users" schemaName="${schemaName}"/>
        </rollback>
    </changeSet>
</databaseChangeLog>