{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kibana-service.fullname" . }}
  labels:
    {{- include "kibana-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kibana-service.selectorLabels" . | nindent 4 }}
{{- end }}
